# Handoff Document

## Purpose

记录本轮 `test-strategy-planner` 规则调整，供后续继续审核、提交或重新生成报告。

## Completed Work

- 将正式报告文件名统一为：
  - `ST-{scope}-测试策略_v{version}_{date}.md`
  - `ST-{scope}-测试案例对照表_v{version}_{date}.md`
- 将主报告第 2 节由“覆盖重点/后续执行”精简为 `层级 | 分层原则`。
- 将主矩阵的 `覆盖结论` 与 `待补内容` 合并为 `覆盖结论与缺口`。
- 将自测检查点明细改为只判断现有案例覆盖，新增“待补充案例列表”。
- 增加待补充案例回写正式测试案例文档的确认门禁，未确认前不分配正式编号、不修改源文档。
- 增加人工验证确认门禁：识别到 UI/M 候选后必须询问，未确认前不读取人工证据规则、不创建 evidence。
- 已运行 skill validator 和 `git diff --check`，校验通过。

## Pending Tasks

- 按用户要求先完成 handoff，再提交当前 planner 规则改动。
- 提交时只纳入本轮修改的 planner 文件，不纳入两份未跟踪的旧命名 `v3` 报告。
- 用户后续可能需要重新生成报告，验证 `ST-` 命名、人工验证询问和待补充案例回写询问是否真实触发。

## Key Decisions

- B 根据测试案例、需求/设计和逻辑代码决定推荐层级；测试资产只能决定资产层级和覆盖结论。
- C 自测检查点分析现有案例是否覆盖，补充案例建议独立列出，不伪造正式案例编号。
- 人工验证和待补充案例回写都必须先询问用户，不能默认启用或默认回写。
- E 只做输出结构契约校验，不重新分析 B/C/D。

## Relevant Artifacts

- Skill：`.agents/skills/test-strategy-planner/SKILL.md`
- 输出模板：`.agents/skills/test-strategy-planner/references/output-template.md`
- 输出规则：`.agents/skills/test-strategy-planner/references/report-output.md`
- 分层策略：`.agents/skills/test-strategy-planner/references/case-layering-strategy.md`
- 自测检查点：`.agents/skills/test-strategy-planner/references/self-checkpoint-review.md`
- 人工验证：`.agents/skills/test-strategy-planner/references/manual-evidence.md`
- 当前未跟踪报告：`reports/test-strategy/` 下两份 T1-US1.5-US1.8 `v3` 文件，暂不纳入提交。
- 最近已推送提交：`c37d23b chore(lark): add approval scopes to login helper`、`9d32bf6 docs(test-strategy): 优化增量分析与断点续跑规则`。

## Suggested Skills for Next Session

- `test-strategy-planner`：继续验证或生成策略报告。
- `handoff`：跨会话交接时继续更新状态。

## Context

当前工作区还有 planner 规则的未提交修改，主要涉及 `SKILL.md`、`case-layering-strategy.md`、`manual-evidence.md`、`output-template.md`、`report-output.md` 和 `self-checkpoint-review.md`。另有两份用户已有的未跟踪 `v3` 报告，提交时必须保持不变。
