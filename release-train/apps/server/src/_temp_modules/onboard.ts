// Legacy file, replaced by modules/trains/routes/operations.ts
import { FastifyInstance } from 'fastify';

export async function onboardRoutes(fastify: FastifyInstance) {
  // No-op, replaced by new implementation
}
