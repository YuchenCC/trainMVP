# T2-US2.3 班次列表查询与火车切换 测试策略与覆盖分析报告

## 1. 分析范围

| 项目 | 内容 |
|---|---|
| 分析范围 | Task2 版本火车管理：US2.3 班次列表查询与火车切换 |
| 分析前确认 | 用户明确要求不复用旧报告，本次分析基于最新代码和测试资产重新生成 |
| 测试案例来源 | `03-需求与设计/Task2-版本火车管理/测试案例/RT-T2-US2.3-班次列表查询与火车切换-测试案例_v1.1_20260516.md` |
| 需求/设计来源 | `03-需求与设计/Task2-版本火车管理/US详细设计/RT-T2-US2.3-版本火车列表-详细设计_v1.1_20260516.md` |
| 代码范围 | 后端 `release-train/apps/server/src/modules/trains/routes/schedule.ts`、`release-train/apps/server/src/modules/trains/services/train.service.ts`；前端 `release-train/apps/web/src/pages/trains/schedules/index.tsx` |
| 测试资产范围 | `t2-us2.3-schedule-api.test.ts`、`train.service.unit.test.ts`、`t2-us2.3-schedules-list.test.ts` |
| 自测检查点来源 | `.agents/skills/test-strategy-planner/references/self-test-checkpoints.md` |
| planner 执行人 | 技术 owner |
| 输入清单完整性 | 完整 |
| 口径差异 | 用户故事 v2.0 原始要求包含"用户相关火车过滤、默认选中相关火车"；本轮确认暂不纳入。当前以 `/schedules` 班次列表页为入口 |
| 存量报告处理 | 不复用旧报告结论，全新分析 |

---

## 2. 测试案例到代码索引

| 测试案例 | 锚点 | 候选代码 | 相关性 | 是否已读 | 下一步 |
|---|---|---|---|---|---|
| API-01~03 | `GET /api/schedules`、分页、`createdAt` 倒序、返回字段 | `routes/schedule.ts:94-110`、`train.service.ts:874-934` | 强相关 | 是 | L1/L2 已覆盖，补分页边界值和参数校验 |
| API-04~05 | `GET /api/trains/:trainId/schedules`、火车存在校验 | `routes/schedule.ts:218-236`、`train.service.ts:826-872` | 强相关 | 是 | L1/L2 已覆盖，确认"不包含其他火车"断言 |
| API-06~08 | 未登录、创建权限、状态变更权限 | `routes/schedule.ts` 认证/RBAC、`rbacMiddleware` | 强相关 | 是 | L2 已覆盖，统一错误响应口径；补水平越权 |
| FE-01~04 | `/schedules`、火车下拉、切换、清空 | `schedules/index.tsx:98-191` | 强相关 | 是 | FE-03/04 缺业务断言，需补列表内容和请求路径验证 |
| FE-05~09 | 表格字段、详情跳转、新增/编辑弹窗 | `schedules/index.tsx:288-468` | 强相关 | 是 | FE-05/09 缺失，FE-08 缺字段断言 |
| FE-10~12 | 状态按钮：开始、封板、投产、取消、编辑 | `schedules/index.tsx:360-444` | 强相关 | 是 | L3 已覆盖按钮可见性，补 loading 状态和二次确认验证 |
| GAP-01~03 | 所属火车列、指定火车分页、角色按钮显隐 | 前端表格列定义、`listTrainSchedules`、角色权限模型 | 强相关 | 是 | GAP-03 为高风险，需优先处理 |

---

## 3. 分层测试方案

### 3.1 L1 后端单测

| 覆盖重点 | 现有覆盖 | 缺口 | 建议 |
|---|---|---|---|
| `listAllSchedules` 分页计算 | ✅ 默认参数、自定义参数、空结果集 | 缺 `pageSize` 最大值边界、`pageSize=0`、负数参数 | 补单测验证边界值处理 |
| `listAllSchedules` 排序与聚合 | ✅ `createdAt` 倒序、容量聚合 | 无 | - |
| `listTrainSchedules` 火车校验 | ✅ 火车存在/不存在、空列表 | 缺"只返回该火车班次"显式断言 | 补 Mock 验证返回数据过滤 |
| `updateTrainScheduleStatus` 状态机 | ✅ 6 种合法/非法流转 | 缺事务副作用验证（需求状态同步） | 补 Mock 验证 `requirement.updateMany` 调用 |
| 事务异常回滚 | ❌ 未测试 | 缺异常场景下事务回滚验证 | 补单测验证异常回滚 |
| 幂等性验证 | ❌ 未实现 | 缺幂等机制实现和测试 | 评估并实现幂等性 |
| 分页参数边界值 | ✅ `page=0` 容错 | 缺 `pageSize=0`、负数、超大值 | 补边界值单测 |

### 3.2 L2 API 测试

| 覆盖重点 | 现有覆盖 | 缺口 | 建议 |
|---|---|---|---|
| 认证 | ✅ 未登录返回 `success: false` | 统一错误响应口径 | 确认项目规范 |
| 垂直权限 | ✅ BA 角色无创建/状态变更权限 | 缺水平越权测试 | 补越权测试用例 |
| 分页 | ✅ 默认参数、自定义参数、边界值 | 缺深度分页性能风险验证 | 补 `page` 超大值测试 |
| 排序 | ✅ `createdAt` 倒序 | 无 | - |
| 字段契约 | ⚠️ 仅断言部分字段 | 缺 `totalCapacity`、`usedCapacity`、`requirementCount` 全字段断言 | 补全字段验证 |
| 前后端校验一致性 | ✅ Schema 验证 | 缺前端参数校验同步验证 | 验证前后端校验一致性 |
| 防重发 | ❌ 未实现 | 缺防重发机制测试 | 与幂等性一并评估 |
| XSS/SQL 注入 | ✅ Prisma ORM 保护 | 缺显式安全测试 | 补安全输入测试 |
| 日志脱敏 | ❌ 未验证 | 缺日志输出审查 | 人工审查日志 |

### 3.3 L3 UI 自动化

| 覆盖重点 | 现有覆盖 | 缺口 | 建议 |
|---|---|---|---|
| 页面加载 | ✅ 标题、下拉框、表格 | 缺分页器、数据量断言 | 补分页器可见性和数据计数 |
| 火车切换 | ✅ 选择动作 | 缺列表内容变化、请求路径断言 | 补请求验证 |
| 清空筛选 | ✅ 清空动作 | 缺恢复全局列表、分页总数恢复断言 | 补请求和分页验证 |
| 新增/编辑弹窗 | ✅ 弹窗打开 | 缺字段断言 | 补弹窗字段验证 |
| 状态按钮 | ✅ 按钮可见性 | 缺 loading 状态、二次确认弹窗 | 补 loading 和确认弹窗验证 |
| 角色按钮显隐 | ⚠️ BA 用户用例与实现冲突 | 前端无角色判断 | 实现前端角色显隐或更新测试预期 |
| 敏感信息脱敏 | ❌ 未验证 | 缺敏感信息展示验证 | 补脱敏验证 |

### 3.4 自测检查点映射

| 检查点 | 关联功能 | 当前覆盖 | 建议措施 |
|---|---|---|---|
| 提交/确认按钮添加 loading，防止重复点击 | 状态变更按钮 | ⚠️ 前端代码有 `modalLoading`，但 L3 未验证 | 补 Playwright 用例验证 |
| 查询语句含分页、且索引生效的执行计划自测用例 | `listAllSchedules` 分页 | ✅ L1 单测分页逻辑 | 添加 EXPLAIN 执行计划人工验证 |
| 重要参数校验（涉及索引命中、特殊字符转译） | 分页参数、`trainId` 参数 | ✅ Schema 验证 | 补特殊字符 `trainId` 测试 |
| 记账交易是否正确记录事务处理的中间状态 | 状态变更事务 | ⚠️ L1 Mock 未验证事务副作用 | 补单测验证 `requirement.updateMany` 调用 |
| 幂等标识有效期测试 | 状态变更接口 | ❌ 未实现幂等机制 | 评估并实现幂等性 |
| 使用 IO 流写文件完成后及时关闭 | 本模块无文件操作 | N/A | - |
| 导出时需限制最大记录数 | 本模块无导出功能 | N/A | - |
| 参数设计边界值自测用例 | 分页参数、`trainId` | ⚠️ 部分覆盖 | 补全边界值用例 |
| 开关类功能按预期效果生效的用例 | 状态按钮显隐、新增班次前置条件 | ⚠️ 部分覆盖 | 补角色按钮显隐和前置条件验证 |
| 前后端校验规则一致 | 分页参数校验 | ✅ Schema 验证 | 验证前后端一致性 |
| 报文格式/长度/加密规则 | API 响应格式 | ✅ 统一 ApiResponse<T> 格式 | 确认加密需求 |
| 防重发（Nonce、Timestamp、签名） | 状态变更接口 | ❌ 未实现 | 与幂等性一并评估 |
| 正常流程测试 | 所有 API 接口 | ✅ L2 基本覆盖 | 保持 |
| 边界条件测试 | 分页参数、`trainId` | ⚠️ 部分覆盖 | 补全边界值 |
| 错误处理测试 | 所有 API 接口 | ✅ L2 已覆盖 | 统一错误响应规范 |
| 敏感操作二次确认 | 状态变更按钮 | ✅ Modal.confirm 已实现 | 补 Playwright 用例验证 |
| 用户、角色、权限控制 | 认证、RBAC | ✅ L2 已覆盖 | 补水平越权测试 |
| 数据不一致（事务问题） | 状态变更事务 | ⚠️ 事务已实现 | 补事务完整性单测 |
| 状态流转验证 | 班次状态机 | ✅ L1 已覆盖 | 保持 |
| 无效状态跳转拦截 | 班次状态机 | ✅ L1 已覆盖 | 保持 |
| 异常回滚/补偿机制 | 状态变更事务 | ⚠️ 事务已实现 | 补异常回滚单测 |
| 分页参数最大值限制 | `pageSize` 限制 | ✅ Schema 限制 100 | 保持 |
| 越权风险（水平/垂直） | API 权限 | ✅ L2 已覆盖垂直越权 | 补水平越权测试 |
| 日志敏感信息脱敏 | 所有日志输出 | ❌ 未验证 | 人工审查日志 |

---

## 4. 覆盖现状

| 范围 | 案例数 | 已覆盖 | 部分覆盖 | 缺失 | 可复用测试资产 | 主要缺口 |
|---|---:|---:|---:|---|---|---|
| 后端 API | 8 | 8 | 0 | 0 | `t2-us2.3-schedule-api.test.ts` | 全字段断言不足；错误响应口径未统一；缺水平越权 |
| 后端 L1 支撑逻辑 | 3 组 | 3 | 0 | 0 | `train.service.unit.test.ts` | 事务副作用验证缺失；分页边界值不全；幂等性未实现 |
| 前端功能 | 12 | 5 | 5 | 2 | `t2-us2.3-schedules-list.test.ts` | FE-03/04 缺业务断言；FE-05/09 缺失；loading/二次确认未验证 |
| 差距验证 | 3 | 0 | 1 | 2 | Playwright BA 权限用例 | GAP-03 前端角色按钮显隐与实现冲突 |

---

## 5. 需求/案例覆盖矩阵

| 需求点/案例组 | 测试案例 | 推荐层级 | 当前覆盖判断 | 待补内容 | 优先级 |
|---|---|---|---|---|---|
| 全局班次列表与分页 | API-01、API-02、API-03、FE-01 | L1 + L2 + L3 | API/service 已覆盖；FE-01 只断言标题、下拉、表格 | FE-01 补分页器、数据量断言；API-01 补全字段断言 | P0 |
| 指定火车班次列表 | API-04、API-05、FE-02、FE-03 | L1 + L2 + L3 | API/service 已覆盖；FE-03 只完成选择动作 | 补选中火车后的列表内容或请求路径断言 | P0 |
| 清空火车筛选 | FE-04 | L3 | 部分覆盖，已执行清空动作但未断言恢复全局列表 | 补 `GET /api/schedules` 请求和分页总数恢复断言 | P1 |
| 字段展示与所属火车列 | FE-05、GAP-01 | L3/M | 缺失。表格未展示"所属火车"列 | 决定是否实现该列；补列标题和值断言 | P1 |
| 详情跳转 | FE-06 | L3 | 部分覆盖，依赖环境数据 | 固化测试数据，确保跳转断言不被空态绕开 | P0 |
| 新增班次入口 | FE-07、FE-08 | L3 | FE-07 已覆盖；FE-08 仅断言弹窗标题 | FE-08 补班次名称、日期字段断言 | P0 |
| 编辑班次弹窗 | FE-09 | L3 | 缺失 | 新增编辑按钮、弹窗标题、字段回填断言 | P1 |
| 状态按钮显示 | FE-10、FE-11、FE-12 | L1 + L2 + L3 | service 状态机已覆盖；L3 有按钮可见性断言 | 固化测试数据；补按钮 loading 状态和二次确认验证 | P0 |
| 后端认证与权限 | API-06、API-07、API-08 | L2 | 已覆盖未登录、非管理员创建、非管理员状态变更 | 统一错误响应口径；补水平越权测试 | P0 |
| 指定火车分页一致性 | GAP-02 | L2 + L3 | 缺失，当前代码明确不支持 | 若纳入实现，补接口分页参数和前端分页回归 | P1 |
| 前端角色按钮显隐 | GAP-03 | L1 前端 + L3 | 高风险缺口。页面无角色判断，与 BA 用例冲突 | 实现前端角色显隐逻辑或更新测试预期 | P0 |
| 幂等性 | 新增 | L1 + L2 | 缺失，未实现幂等机制 | 评估并实现幂等性；补单测和 API 测试 | P0 |
| 事务完整性 | 新增 | L1 | 部分覆盖，缺副作用验证和异常回滚 | 补事务副作用和异常回滚单测 | P0 |

---

## 6. 代码影响测试矩阵

| 文件/模块 | 影响逻辑 | 影响案例 | 推荐层级 | 必须覆盖原因 | 后续 skill |
|---|---|---|---|---|---|
| `routes/schedule.ts` | `/api/schedules`、`/api/trains/:trainId/schedules`、创建/取消/状态变更路由、认证和 RBAC | API-01~08 | L2 | 接口契约、认证、权限需要 HTTP 链路验证 | `automating-api-testing` |
| `train.service.ts` | 分页计算、倒序排序、字段聚合、火车存在校验、状态机、事务副作用 | API-01~05、API-08、FE-10~12 | L1 + L2 | 业务规则可隔离，单测覆盖核心函数；事务完整性需 L1 验证 | `unit-test-governance` |
| `schedules/index.tsx` | 页面加载、火车下拉、切换/清空、分页状态、列定义、详情跳转、新增/编辑弹窗、状态按钮 | FE-01~12、GAP-01~03 | L3 + L1 前端 | 用户可见行为和角色显隐风险集中在页面组件 | `webapp-testing` |
| `redis/幂等中间件` | 幂等标识生成、存储、有效期检查 | 状态变更接口 | L1 + L2 | 状态变更为核心交易，幂等性为 P0 要求 | `unit-test-governance` + `automating-api-testing` |

---

## 7. 现有测试资产判断

| 现有测试文件 | 可复用覆盖 | 不足 | 处理建议 |
|---|---|---|---|
| `t2-us2.3-schedule-api.test.ts` | API-01~08 基本覆盖；包含列表、分页、排序、指定火车、认证和权限 | 错误响应按 200 + `success=false` 断言；API-01 字段断言不全；缺水平越权测试 | 保留并补强全字段断言；确认错误响应规范；补越权测试 |
| `train.service.unit.test.ts` | 覆盖 `listAllSchedules`、`listTrainSchedules`、`updateTrainScheduleStatus` | 不覆盖 API 契约和前端行为；事务副作用 Mock 验证不足；分页边界值不全；无幂等性测试 | 作为 L1 支撑证据，补强事务验证、边界值和幂等性 |
| `t2-us2.3-schedules-list.test.ts` | 覆盖 FE-01~04、FE-06~08、FE-10~12，并有额外刷新和 BA 权限用例 | FE-03/04 缺业务断言；FE-05/09 缺失；FE-15 与实现冲突；loading 状态未验证；二次确认未验证 | 补强断言；新增 FE-05/09 用例；修复 GAP-03 冲突；补 loading 和二次确认验证 |

---

## 8. TODO 清单与后续执行

| 优先级 | TODO | 推荐 skill | 输入 | 预期输出 |
|---|---|---|---|---|
| P0 | 修复 GAP-03：前端角色按钮显隐。当前页面无角色判断，BA Playwright 用例与实现冲突 | `unit-test-governance` + `webapp-testing` | `schedules/index.tsx`、角色权限模型、现有 Playwright 用例 | 前端 helper 单测、UI 自动化证据 |
| P0 | 补强状态按钮 loading 验证：开始、封板、投产、取消按钮点击后显示 loading | `webapp-testing` | `schedules/index.tsx`、现有 Playwright 用例 | loading 状态验证用例 |
| P0 | 补强敏感操作二次确认验证：状态变更按钮点击后显示确认弹窗 | `webapp-testing` | `schedules/index.tsx`、现有 Playwright 用例 | 二次确认弹窗验证用例 |
| P0 | 补强 FE-03/FE-04：火车切换和清空筛选必须有请求路径/列表内容/分页总数断言 | `webapp-testing` | `t2-us2.3-schedules-list.test.ts` | 稳定 UI 回归用例 |
| P0 | 固化测试数据：确保 FE-06/FE-10~12 不依赖环境已有数据 | `webapp-testing` | Playwright 测试数据、后端 seed API | 独立测试数据准备脚本 |
| P0 | 统一 API 错误响应口径：确认项目规范是保持 200 + `success=false` 还是改为 HTTP 401/403 | `automating-api-testing` | `schedule.ts`、API 测试、统一错误处理中间件 | API 契约补充或文档修订 |
| P0 | 补强 L1 事务副作用验证：验证状态变更时 `requirement.updateMany` 正确调用 | `unit-test-governance` | `train.service.unit.test.ts` | 事务副作用单测 |
| P0 | 补强 L1 事务异常回滚验证：模拟事务执行中异常，验证数据回滚 | `unit-test-governance` | `train.service.unit.test.ts` | 事务回滚单测 |
| P0 | 评估并实现幂等机制：状态变更接口添加幂等性（Redis + requestId） | `unit-test-governance` + `automating-api-testing` | `train.service.ts`、Redis 配置 | 幂等中间件、单测、API 测试 |
| P0 | 补水平越权测试：验证用户不能查看/操作非自己管理的班次 | `automating-api-testing` | `schedule.ts`、权限模型 | 越权场景用例 |
| P0 | 补分页边界值单测：`pageSize=0`、负数、超大值、`pageSize > 100` | `unit-test-governance` | `train.service.unit.test.ts` | 边界值单测 |
| P1 | 补 FE-05 表格字段断言，并确认是否实现"所属火车"列 | `webapp-testing` | 表格列定义、GAP-01 需求 | 字段覆盖证据或差距闭环 |
| P1 | 补 FE-09 编辑班次弹窗用例 | `webapp-testing` | 页面代码、测试数据 | 编辑入口覆盖证据 |
| P1 | API-01 补全字段断言：`totalCapacity`、`usedCapacity`、`requirementCount` | `automating-api-testing` | `t2-us2.3-schedule-api.test.ts` | 全字段契约验证 |
| P1 | 添加 EXPLAIN 执行计划人工验证步骤，确认分页查询索引生效 | M | `train.service.ts`、数据库 | 索引验证证据 |
| P1 | 人工审查日志输出，确认敏感信息已脱敏 | M | 后端日志配置、测试日志输出 | 日志脱敏审查报告 |

---

## 9. 后续 skill 动态推荐

| 后续任务 | 推荐 skill | 是否存在 | 推荐原因 | 输入建议 |
|---|---|---|---|---|
| 单测门禁/增量覆盖率 | `unit-test-governance` | 是 | 项目已有统一单测门禁 skill，适合重新判定后端增量覆盖和前端单测触发建议 | 本报告、变更文件、coverage 报告 |
| API/JMeter 覆盖 | `automating-api-testing` | 是 | 项目已有 API 自动化 skill，适合校准接口契约、认证、权限和错误响应 | `schedule.ts`、API 测试文件、错误响应规范 |
| UI 自动化 | `webapp-testing` | 是 | 项目已有 Playwright 测试 skill，适合补强 `/schedules` 页面断言、loading 和二次确认验证 | 页面代码、现有 e2e 文件、测试数据 |
| 最终覆盖报告 | `test-report-generator` | 是 | 项目已有测试报告生成 skill，可汇总 vitest coverage 与 Playwright 报告 | 本报告、单测治理报告、测试执行输出 |

---

## 10. 风险提醒

| 风险 | 影响 | 建议 |
|---|---|---|
| 前端角色按钮显隐缺口与现有 BA 用例冲突 | 测试可能失败；非管理员会看到管理操作，依赖后端兜底 | P0 修复前端角色显隐，或明确本轮冻结为后端兜底 |
| 状态变更接口未实现幂等性 | 网络抖动可能导致重复提交，状态被多次变更 | P0 评估并实现幂等性（Redis + requestId） |
| 事务副作用未充分验证 | 状态变更时关联需求状态更新可能失败，导致数据不一致 | P0 补 L1 单测验证事务完整性 |
| 状态变更按钮缺少 loading 和二次确认验证 | 用户可能重复点击导致重复提交；误操作无法撤销 | P0 补 Playwright 用例验证 |
| 指定火车列表不分页 | 火车下班次多时页面一次加载全部，和全局列表体验不一致 | P1 评估是否纳入实现，纳入则补 L2/L3 |
| UI 用例断言偏弱 | 可能"操作执行了但业务结果没验证" | 补请求、列表内容、分页、字段断言 |
| 错误响应口径未统一 | 文档/测试/实现对 HTTP 状态码理解不一致 | 统一为项目规范的响应格式 |
| 水平越权测试缺失 | 用户可能越权查看或操作非自己管理的班次 | P0 补越权测试用例 |

---

*生成时间：2026-06-30*  
*基于最新代码和自测检查点重新分析，不复用旧报告结论*