# T1-US1.5-US1.8 测试案例测试方式对照表

## 1. 统计概览

| 测试方式 | 数量 | 说明 |
|---|---:|---|
| L1 后端单测 | 13 | 本轮后端逻辑案例均纳入；批量案例当前缺少实现闭包 |
| L2 API/JMeter | 13 | 状态、权限、错误响应、审计或跨操作链需要 API 证据 |
| L3 UI 自动化 | 9 | 涉及按钮显隐、操作区或自动进入编辑页的案例 |
| M 人工验证 | 不纳入 | 用户已确认本轮不纳入人工验证 |

## 2. 对照表

| 案例 | 测试目标 | 推荐层级 | 分层理由 | 资产层级 | 覆盖结论 | TODO |
|---|---|---|---|---|---|---|
| TC1.5.1 正常发起评审 | 草稿→待评审 | L1 后端单测 + L2 API | 状态、权限、乐观锁、审计及接口契约 | L2 API + L3 UI | 部分覆盖：缺 L1 后端单测；测试案例正文仅有索引 | 补 L1 submitReview 状态/权限/事务/审计单测 |
| TC1.5.2 非草稿状态不显示按钮 | 状态门禁和按钮显隐 | L1 后端单测 + L2 API + L3 UI | 后端拒绝非法状态，UI 验证按钮显隐 | L2 API | 部分覆盖：仅后端状态拒绝，缺 UI 显隐和 L1 | 补 L1 状态门禁及 L3 按钮显隐 |
| TC1.5.3 非 BA 角色不显示按钮 | 权限拒绝和按钮显隐 | L1 后端单测 + L2 API + L3 UI | service、RBAC、UI 三层职责不同 | L2 API | 部分覆盖：缺 UI 显隐和 L1；资产编号与源索引语义不完全一致 | 补 L1 权限矩阵及 L3 按钮显隐 |
| TC1.5.4 必填字段不完整提示 | 必填校验和具体提示 | L1 后端单测 + L2 API + L3 UI | service 校验、API 错误契约、页面提示 | L2 API | 部分覆盖：只覆盖标题/描述，缺 4 类字段、UI 提示和 L1 | 补字段边界 L1/L2 及 L3 提示 |
| TC1.5.5 发起评审后不可编辑 | 状态流转后编辑门禁 | L1 后端单测 + L2 API + L3 UI | submitReview 与 updateRequirement 的状态闭环 | 无相关资产 | 缺失：无直接编辑门禁或操作区证据 | 补 L1 updateRequirement 门禁、L2 接口和 L3 操作区 |
| TC1.5.6 审计日志记录 | SUBMIT_REVIEW 日志 | L1 后端单测 + L2 API | 事务副作用和查询结果 | L2 API | 部分覆盖：有 SUBMIT_REVIEW 日志断言，缺 L1 | 补 L1 审计字段有效断言 |
| TC1.5.7 批量发起评审 | 批量状态流转 | L1 后端单测 + L2 API + L3 UI | 案例有要求，但当前未发现批量实现 | 无相关资产 | 缺失：批量实现和测试资产均缺 | 确认批量功能后补实现、L1/L2/L3 |
| TC1.5.8 批量含非草稿 | 批量部分失败策略 | L1 后端单测 + L2 API | 批量状态门禁与响应契约 | 无相关资产 | 缺失：批量部分失败策略和测试资产均缺 | 确认批量失败策略后补实现和测试 |
| TC1.8.1 正常重新编辑 | 已拒绝→草稿并进入编辑流程 | L1 后端单测 + L2 API + L3 UI | 状态、权限、乐观锁、审计、页面跳转 | L2 API + L3 UI | 部分覆盖：API 与完整 UI 链路存在，缺 L1 | 补 L1 reEdit 状态/权限/事务/审计单测 |
| TC1.8.2 非已拒绝状态不显示按钮 | 状态门禁和按钮显隐 | L1 后端单测 + L2 API + L3 UI | 后端状态门禁与前端显隐分别验证 | L2 API | 部分覆盖：有 API 非法状态拒绝，缺 UI 显隐和 L1 | 补 L1 状态门禁及 L3 按钮显隐 |
| TC1.8.3 重新编辑后审计记录 | RE_EDIT 日志 | L1 后端单测 + L2 API | 事务副作用和查询结果 | L2 API | 部分覆盖：有 RE_EDIT 日志断言，缺 L1 | 补 L1 审计字段有效断言 |
| TC1.8.4 重新编辑后可再次发起评审 | REJECTED→DRAFT→PENDING_REVIEW | L1 后端单测 + L2 API + L3 UI | 跨操作状态链和页面闭环 | L3 UI | 部分覆盖：有 Playwright 链路，缺 L1 和独立 L2 链路 | 补 L1 状态链及 L2 API 闭环 |
| TC1.8.5 PM 角色可重新编辑 | PM 权限 | L1 后端单测 + L2 API + L3 UI | service、RBAC、按钮显隐分别验证 | L2 API | 部分覆盖：有 PM API 成功，缺 L1/UI，需核对 BA 归属人 | 补 L1 权限和 L3 显隐；统一 BA 规则 |

## 3. 测试资产计划

| 测试层级 | 新建/复用 | 测试资产 | 覆盖案例/检查点 | 说明 |
|---|---|---|---|---|
| L1 后端单测 | 新建 | `apps/server/src/modules/requirements/submit-review.unit.test.ts`、`re-edit.unit.test.ts` | TC1.5.1-TC1.5.8、TC1.8.1-TC1.8.5 及自测检查点 | 项目已有 `service.test.ts`，但未覆盖本次两个 service，需补有效断言 |
| L2 API | 复用并补充 | `apps/server/src/__tests__/t1-us1-requirement-entry.test.ts`、`t1-requirements-api-automation.test.ts` | 状态、权限、错误、审计、版本冲突 | 已读取为 app.inject API/集成测试；批量和编辑门禁仍需补充 |
| L3 UI | 复用并补充 | `apps/web/tests/e2e/journey-1-create-review-pass.test.ts` | TC1.8.1、TC1.8.4 及按钮显隐/提示 | Playwright 已覆盖一条完整拒绝后重新编辑链路；按钮 loading/隐藏缺专门断言 |

## 4. 已确认的人工验证替代 UI 场景

用户已确认本轮不纳入人工验证；已发现 UI 场景，但本轮不创建 evidence，后续以 L3 自动化补充。

## 附录

### A. 自测检查点完整审查记录

模块 C 已完成 92 个叶子检查点遍历；模块 D 已读取相关测试资产，完整记录如下。本轮未执行测试命令。

| 检查点类别路径 | 检查点原文 | 原文优先级 | 原文测试层次 | 代码证据 | 涉及判定 | 判定理由 | 关联案例 | 案例覆盖判断 | 补充案例建议 | 测试资产证据状态 | TODO |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 一、交互类 / 1. 接口 | 前后端校验规则是否一致，字段关联、校验规则（必填/格式/范围）（如前端允许 11 位手机号，后端仅允许 10 位） | P0 | L2 | service.ts:1134-1155；BR1.1.1/BR1.5.3；前端校验未纳入。 | 待确认 | 代码仅覆盖后端 service 字段校验；前端规则未纳入，且需求规则与实现有差异。 | TC1.5.4 | 待确认：前端、案例正文或安全口径未完整纳入 | 建议-CP-001：补充前后端必填/长度/范围规则对照，L1 + L2。 | L2 API：标题/描述缺失；未覆盖前端规则与其余字段 | 确认前端校验与后端 2-100 / 1-200 的统一口径。 |
| 一、交互类 / 1. 接口 | 调用接口需配置合理的超时时间 | P1 | L2 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 一、交互类 / 1. 接口 | 报文格式/长度/加密规则 | P0 | L2 | requirements/index.ts:282-295、338-351；service.ts 返回 RequirementDetail。 | 涉及 | 路由返回 ApiResponse，但未发现专用 schema/加密规则。 | TC1.5.1、TC1.8.1 | 待确认：测试案例正文只有编号和名称，无法核对步骤与预期结果 | 建议-CP-002：补充成功响应、错误响应和字段边界契约，L2 API。 | L2 API：成功/错误响应断言；无专用 schema 断言 | 核对响应字段和错误结构契约。 |
| 一、交互类 / 1. 接口 | 防重发，如 Nonce、Timestamp、签名验证加频率限制 | P0 | L2 | service.ts:1120-1175、1585-1611。 | 涉及 | 状态门禁和 version 条件更新可拦截重复/并发状态变更；未发现 nonce、签名或频率限制。 | TC1.5.1、TC1.8.1 | 待确认：测试案例正文只有编号和名称，无法核对步骤与预期结果 | 建议-CP-003：补充重复提交和并发提交，L1 + L2。 | L2 API：状态门禁/重复请求；未形成真正并发测试 | 确认是否需要独立幂等键/防重策略。 |
| 一、交互类 / 1. 接口 | 调用频率限制，限流/熔断/开关等 | P1 | L2 + Perf | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 一、交互类 / 1. 接口 | 正常流程测试：确保接口在标准输入下返回正确的输出 | P0 | L2 | index.ts:282-295、338-351；service.ts:1095-1201、1567-1636。 | 涉及 | 存在发起评审和重新编辑的标准 API 路由及成功返回。 | TC1.5.1、TC1.8.1 | 待确认：测试案例正文只有编号和名称，无法核对步骤与预期结果 | 无 | L2 API + L3 UI：路由成功和两条用户旅程 | 已读取相关 L2 API 资产；本轮未执行测试。 |
| 一、交互类 / 1. 接口 | 边界条件测试：测试接口在参数取最大值、最小值、空值、特殊字符等极限情况下的表现 | P0 | L2 | service.ts:1134-1155；title 为 1-200，storyPoints 为 1-100。 | 涉及 | service 校验空值、长度和数值边界；案例正文没有边界步骤明细。 | TC1.5.4 | 待确认：测试案例正文只有编号和名称，无法核对步骤与预期结果 | 建议-CP-004：补充六项字段空值和边界，L1 + L2。 | L2 API：标题/描述空值；缺其余字段边界 | 统一标题 2-100 与代码 1-200 的规则。 |
| 一、交互类 / 1. 接口 | 错误处理测试：与提供方系统确认接口返回的特殊异常码（包括查无记录等），验证接口返回的各种错误码，有做出相应的正确处理逻辑 | P0 | L2 | service.ts:1116-1155、1168-1175、1581-1611。 | 涉及 | service 抛出不存在、状态、权限、字段和版本冲突业务错误，路由交由统一错误处理。 | TC1.5.1、TC1.5.2、TC1.5.3、TC1.5.4、TC1.8.1、TC1.8.2 | 待确认：测试案例正文只有编号和名称，无法核对步骤与预期结果 | 建议-CP-005：补充异常分支到 HTTP 状态和错误码映射，L2 API。 | L2 API：状态/权限/字段/不存在错误断言 | 已读取错误响应断言；本轮未执行测试。 |
| 一、交互类 / 1. 接口 | 重试机制测试：验证接口是否能够正确重试 | P1 | L2 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 一、交互类 / 2. 交互 | 提交/确认按钮添加 loading，防止重复点击 | P0 | L3 | 本轮只有后端 service/index/shared constants，未发现 loading 实现。 | 待确认 | 测试案例涉及按钮防重复，但本轮未纳入前端页面代码。 | TC1.5.1、TC1.8.1 | 待确认：前端、案例正文或安全口径未完整纳入 | 建议-CP-006：补充连续点击场景，L3；改用人工验证需用户确认。 | L3 UI：有旅程点击；未见 loading 断言 | 确认前端测试或人工验证范围。 |
| 一、交互类 / 2. 交互 | 接口异常时仅返回业务必要的报错信息，并针对各类报错做友好的异常处理 | P1 | L3 | 后端返回业务错误；前端提示映射未纳入。 | 待确认 | 测试案例涉及页面错误提示，但本轮未纳入前端错误处理代码。 | TC1.5.4、TC1.5.2、TC1.5.3、TC1.8.2 | 待确认：前端、案例正文或安全口径未完整纳入 | 建议-CP-007：补充错误码到页面提示映射，L3。 | L3 UI：未见专门错误提示断言 | 确认前端错误提示验证方式。 |
| 一、交互类 / 2. 交互 | 敏感操作二次确认，如确认是否删除 | P0 | L3 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 二、管理类功能 / 1. 权限控制 | 用户、角色、权限控制符合业务需求 | P0 | L2 | index.ts:285-288、341-344；service.ts:1125-1132、1589-1594。 | 涉及 | 路由有认证和 RBAC，service 进一步校验角色和资源归属。 | TC1.5.3、TC1.8.5 | 待确认：测试案例正文只有编号和名称，无法核对步骤与预期结果 | 建议-CP-008：补充 BA 归属人、非归属 BA、PM、PROJECT_MGR、TRAIN_ADMIN、SUPER_ADMIN 矩阵，L1 + L2。 | L2 API：角色和归属权限断言 | 统一 US1.8 BA 归属人和 PROJECT_MGR 是否允许。 |
| 二、管理类功能 / 2. 数据操作 | 查询语句含分页、且索引生效的执行计划自测用例，分页逻辑正常（如最后一页翻页空数据、页码越界报错） | P0 | L1 + M | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 二、管理类功能 / 2. 数据操作 | 涉及表数据的任何操作（增删改查），请求内容需与表数据进行准确性核对，涉及批量插入/更新的，还需按照生产数据总量验证处理耗时 | P0 | L1 | service.ts:1158-1200、1596-1635。 | 涉及 | 两个操作更新 Requirement 状态/version 并写入 StatusLog。 | TC1.5.1、TC1.5.6、TC1.8.1、TC1.8.3 | 待确认：测试案例正文只有编号和名称，无法核对步骤与预期结果 | 无 | L2 API：状态、version、日志结果断言 | 已读取状态、version、日志断言；缺 L1 资产。 |
| 二、管理类功能 / 2. 数据操作 | 重要参数校验（比如涉及是否能命中索引的字段、模糊查询未转译特殊字符等），避免一次性加载过多数据 | P0 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 二、管理类功能 / 3. 批量操作 | 是否涉及复杂业务逻辑不拆分、长事务导致数据库死锁 | P2 | M + Perf | shared/constants/index.ts:39、195-197；service/index 未发现 batch submit。 | 待确认 | 测试案例包含批量发起评审，但当前未发现批量 service/route，仅发现常量定义。 | TC1.5.7、TC1.5.8 | 待确认：前端、案例正文或安全口径未完整纳入 | 建议-CP-009：补充批量全成功、含非草稿、部分失败和事务策略，L1 + L2。 | 无相关资产：未发现批量实现或测试 | 先确认批量功能是否属于本轮实现。 |
| 二、管理类功能 / 3. 批量操作 | 打印大量日志或者批量生成临时文件导致资源不足 | P2 | M + Monitor | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 二、管理类功能 / 3. 批量操作 | 数据不一致：1、部分提交或回滚不彻底；2、幻读/不可重复读；3、异步任务导致（如同步 ES、缓存更新策略等） | P0 | L1 | service.ts:1158-1186、1596-1621。 | 涉及 | 状态更新和审计写入位于 Prisma transaction 内，存在事务一致性审查点。 | TC1.5.1、TC1.5.6、TC1.8.1、TC1.8.3 | 待确认：测试案例正文只有编号和名称，无法核对步骤与预期结果 | 建议-CP-010：补充更新成功但日志失败、版本冲突和事务回滚，L1。 | L2 API：正常事务结果；未见故障回滚 | 未发现事务故障注入资产；需补 L1。 |
| 二、管理类功能 / 4. 审计 | 对数据操作是否满足审计要求 | P1 | M | service.ts:1177-1185、1613-1620。 | 涉及 | 两个操作均写入状态审计日志。 | TC1.5.6、TC1.8.3 | 待确认：测试案例正文只有编号和名称，无法核对步骤与预期结果 | 无 | L2 API：审计日志字段断言 | 已读取 API 日志查询断言；缺 L1。 |
| 三、交易类功能 / 1. 数据一致性 | 端到端链路测试，模拟完成交易流程，检查数据库变更情况、检查核心系统数据最终是否一致，并截图 | P0 | L1 + L2 | service.ts:1158-1200、1596-1635。 | 涉及 | 单次操作同时改变状态/version并创建审计日志，形成后端链路；未涉及外部核心系统。 | TC1.5.1、TC1.8.1 | 待确认：测试案例正文只有编号和名称，无法核对步骤与预期结果 | 建议-CP-011：补充状态、version、StatusLog 最终一致性，L1 + L2。 | L2 API + L3 UI：状态落库和用户旅程 | 确认是否需要跨服务端到端链路。 |
| 三、交易类功能 / 2. 金额类型 | 单笔、批量、手续费等计算是否正确 | P0 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 三、交易类功能 / 2. 金额类型 | 金额精度、金额格式验证，是否存在金额溢出或数据类型错误、最大最小值限制、负数、零值、非法字符等 | P0 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 三、交易类功能 / 3. 账务处理验证 | 借贷方向验证：每笔交易的借方与贷方是否正确 | P0 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 三、交易类功能 / 3. 账务处理验证 | 是否遵循"有借必有贷，借贷必相等"原则 | P0 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 三、交易类功能 / 3. 账务处理验证 | 是否正确区分内部账户与外部账户的借贷方向 | P0 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 三、交易类功能 / 3. 账务处理验证 | 确认账务分录验证、余额更新、流水记录验证等 | P0 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 三、交易类功能 / 3. 账务处理验证 | 账务冲正、对账验证，是否支持交易冲正（冲销、撤销等）、是否与外部系统（如银联、支付平台、第三方对账系统）进行对账，结果是否正确 | P0 | L1 + M | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 三、交易类功能 / 4. 状态流转 | 验证所有执行状态、审批状态、交易状态（如"待支付"→"已支付"→"已退款"）的流转是否符合业务规则 | P0 | L1 | service.ts:1120-1123、1160-1165、1585-1587、1597-1602。 | 涉及 | 实现 DRAFT→PENDING_REVIEW、REJECTED→DRAFT 两条状态流转。 | TC1.5.1、TC1.5.2、TC1.8.1、TC1.8.2、TC1.8.4 | 待确认：测试案例正文只有编号和名称，无法核对步骤与预期结果 | 无 | L2 API + L3 UI：正向状态流转证据 | 已读取 API/UI 正向状态证据；缺 L1。 |
| 三、交易类功能 / 4. 状态流转 | 确保无效状态跳转（如"已退款"直接转为"已发货"）被拦截 | P0 | L1 | service.ts:1120-1123、1585-1587。 | 涉及 | 非 DRAFT 和非 REJECTED 状态分别被 service 拦截。 | TC1.5.2、TC1.8.2 | 待确认：测试案例正文只有编号和名称，无法核对步骤与预期结果 | 建议-CP-012：补充所有非法状态逐项拦截，L1。 | L2 API：非法状态拒绝 | 案例正文缺少非法状态枚举，需补齐前置数据。 |
| 三、交易类功能 / 4. 状态流转 | 记账交易是否正确记录事务处理的中间状态，并根据关联事务的状态进行提交确认或回滚 | P0 | L1 | service.ts:1157-1200、1596-1635。 | 涉及 | 事务中先更新状态/version，再写状态日志，异常时依赖事务回滚。 | TC1.5.1、TC1.5.6、TC1.8.1、TC1.8.3 | 待确认：测试案例正文只有编号和名称，无法核对步骤与预期结果 | 无 | L2 API：正常事务顺序结果；无中间状态断言 | 已读取正常落库结果；无中间状态故障证据。 |
| 三、交易类功能 / 5. 异常回滚/补偿机制 | 异常场景模拟测试：验证是否触发回滚（如取消订单、释放库存）或补偿操作（如自动退款） | P0 | L1 | service.ts:1158-1201、1596-1636。 | 涉及 | Prisma transaction 覆盖状态更新和审计写入，可在异常时回滚。 | TC1.5.1、TC1.5.6、TC1.8.1、TC1.8.3 | 待确认：测试案例正文只有编号和名称，无法核对步骤与预期结果 | 建议-CP-013：补充事务内任一步骤失败后的状态和日志回滚，L1。 | 无相关资产：未见事务故障注入 | 确认异常注入方式和回滚断言。 |
| 三、交易类功能 / 5. 异常回滚/补偿机制 | 服务中断恢复测试：重启核心服务后，检查未完成的事务是否通过补偿机制恢复，补偿操作是否幂等 | P2 | M | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 三、交易类功能 / 6. 幂等设计 | 重复请求测试：使用相同参数（如订单号、金额）重复提交关键请求（如支付、退款）。验证系统是否仅处理第一次请求，后续请求返回"已处理"或"操作冲突" | P0 | L1 | service.ts:1160-1175、1597-1611。 | 涉及 | version 条件更新加状态门禁，使重复请求进入非法状态或版本冲突；未发现独立幂等键。 | TC1.5.1、TC1.8.1 | 待确认：测试案例正文只有编号和名称，无法核对步骤与预期结果 | 建议-CP-014：补充同参数重复和并发请求结果码/数据唯一性，L1 + L2。 | L2 API：顺序重复请求；非并发幂等 | 确认重复请求产品口径。 |
| 三、交易类功能 / 6. 幂等设计 | 幂等标识有效期测试：设置幂等标识的存储时间（如 Redis 的 EXPIRE），模拟超时后重复请求是否被允许 | P0 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 三、交易类功能 / 7. 序列号/流水号 | 生成规则是否合理，使用合适成熟的序号生成器算法，比如高低位算法、雪花算法等，根据数据量选择合适的序号长度，避免出现序号不足或长度溢出；分表分库不使用数据库自增机制 | P0 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 三、交易类功能 / 7. 序列号/流水号 | 唯一性保障（分布式防重复）：高并发压力测试、分布式节点测试 | P1 | Perf | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 三、交易类功能 / 7. 序列号/流水号 | 如涉及上下游，还需考虑跨系统格式兼容 | P1 | M | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 三、交易类功能 / 8. 审计 | 交易日志审计，日志要素、存储时间等 | P1 | M | service.ts:1177-1185、1613-1620。 | 涉及 | 状态变更写入 StatusLog，包含操作类型、前后状态和操作人。 | TC1.5.6、TC1.8.3 | 待确认：测试案例正文只有编号和名称，无法核对步骤与预期结果 | 无 | L2 API：StatusLog 字段断言 | 已读取 StatusLog 字段断言；缺 L1。 |
| 四、文件相关 / 1. 文件上传 | 仅允许上传特定类型文件，且不能通过文件后缀进行判断，需检查文件头中的文件类型 | P0 | L2 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 四、文件相关 / 1. 文件上传 | 对上传的文件大小进行限制，一般不得超过 10MB | P0 | L2 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 四、文件相关 / 1. 文件上传 | 上传文件时防止重复提交上传申请 | P1 | L2 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 四、文件相关 / 2. 文件存储 | 同路径下文件数量根据系统确定，不宜过多 | P2 | M + Monitor | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 四、文件相关 / 2. 文件存储 | 文件名防重复设计，避免覆盖 | P1 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 四、文件相关 / 3. 资源释放 | 使用 IO 流写文件完成后，是否及时关闭，避免在高并发情况下造成后续写文件功能异常 | P0 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 四、文件相关 / 3. 资源释放 | 及时清理临时目录和文件或者使用完删除，需要根据文件数量评估清理频率 | P1 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 四、文件相关 / 4. 文件下载 | 不得通过文件的相对路径进行下载，防止通过修改参数获取服务器上文件 | P0 | L2 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 四、文件相关 / 4. 文件下载 | 导出时需限制最大记录数 | P0 | L2 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 四、文件相关 / 4. 文件下载 | 批量下载没有并发控制或限速控制，导致系统资源耗尽 | P1 | Perf | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 五、复杂逻辑 / 1. 代码 / 循环与递归 | 覆盖循环次数的边界值用例（如 0 次、最大次数、数据总量） | P0 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 五、复杂逻辑 / 1. 代码 / 循环与递归 | 递归深度的极限范围用例 | P0 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 五、复杂逻辑 / 1. 代码 / 多线程与异步 | 多线程场景下的线程安全性和数据一致性自测用例 | P1 | L1 + Perf | service.ts:1160-1175、1597-1611。 | 涉及 | 使用 version 条件更新处理并发修改；没有多线程异步任务。 | TC1.5.1、TC1.8.1 | 待确认：测试案例正文只有编号和名称，无法核对步骤与预期结果 | 建议-CP-015：补充两个并发请求仅一个成功及另一个冲突，L1 + L2。 | L2 API：顺序版本场景；非真正并发 | 已读取顺序版本场景；未形成真正并发测试。 |
| 五、复杂逻辑 / 1. 代码 / 多线程与异步 | 高并发场景下异步逻辑的稳定性用例（结合性能测试） | P1 | Perf | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 五、复杂逻辑 / 1. 代码 / 复杂算法（排序、搜索、动态规划等） | 设计不同规模的测试数据（如小数据、大数据）用例 | P0 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 五、复杂逻辑 / 1. 代码 / 复杂算法（排序、搜索、动态规划等） | 算法的输出是否符合预期，性能是否满足要求的自测用例 | P0 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 六、配置类 / 1. 配置文件 | 配置参数对应功能修改前、后的结果差异对比 | P1 | M | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 六、配置类 / 1. 配置文件 | 新增配置文件参数必须附默认值检查，并确认研发测试环境与生产的一致性用例 | P1 | M | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 六、配置类 / 2. 环境变量 | 变量被正确读取和应用的截图 | P1 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 六、配置类 / 3. 应用参数 | 开关类功能按预期效果生效的用例 | P0 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 六、配置类 / 3. 应用参数 | 参数设计边界值自测用例（包括但不限于最大值、最小值、空值） | P0 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 六、配置类 / 4. 脚本 | 生产检查脚本执行的文件权限、空间、目录等 | P1 | M | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 七、数据库 / 1. 库表设计 | 字段类型/长度/默认值、主键/外键约束、唯一索引 | P0 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 七、数据库 / 1. 库表设计 | 索引缺失或冗余 | P1 | M + Tools | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 七、数据库 / 1. 库表设计 | 存量数据变更，需检查生产数据是否需要初始化、满足变更后的要求 | P1 | M + Perf | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 七、数据库 / 1. 库表设计 | 数据表变更对上游、下游无影响自测用例 | P1 | M + Perf | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 七、数据库 / 2. 数据迁移/重构 | 具有备份原始数据、且可正常恢复使用的自测用例 | P2 | M | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 七、数据库 / 2. 数据迁移/重构 | 生产数据总量执行的耗时时长用例 | P1 | Perf | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 七、数据库 / 3. 供数 | 按照定时任务配置的命令进行试推成功用例 | P1 | L1 + M | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 七、数据库 / 3. 供数 | 异常数据是否通过数据核验 | P1 | M | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 七、数据库 / 3. 供数 | 按照定时任务配置的到点供数的用例 | P1 | M | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 七、数据库 / 4. 订阅 | 生产数据总量订阅解析入库用例，订阅执行失败要确保上日数据的可用性 | P1 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 七、数据库 / 4. 订阅 | 对应业务功能可正确使用订阅数据的自测用例 | P1 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 八、定时任务 / 1. 调度设计 | 校验 Cron 表达式有效性（如跨月/跨年场景），检查任务执行时间准确性 | P0 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 八、定时任务 / 2. 容错机制 | 重试次数/间隔、重试终止条件、失败告警、管台手动重跑等 | P1 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 八、定时任务 / 2. 容错机制 | 失败告警 | P1 | L1 + Monitor | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 八、定时任务 / 2. 容错机制 | 管台手动重跑 | P1 | L2 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 八、定时任务 / 3. 异常控制 | 开关是否可以停止正在执行中的定时任务 | P1 | L2 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 八、定时任务 / 3. 异常控制 | 异常数据是否标记跳过，避免异常数据堆积 | P1 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 八、定时任务 / 4. 并发控制 | 双集群、或多活是否有多节点分布式锁控制，避免重复执行 | P0 | L1 + Perf | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 八、定时任务 / 4. 并发控制 | 幂等设计（是否可以重复执行，数据不会重复处理） | P0 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 九、界面类 / 1. 向下兼容 | 含新版本能正确处理旧版本的数据和功能、旧版本升级到新版本后的兼容 | P1 | M | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 九、界面类 / 2. 环境兼容 | 测试不同操作系统（Windows、Linux、macOS）和浏览器版本下的兼容性、屏幕尺寸及分辨率的兼容性测试 | P2 | L3 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 十、安全 / 1. 输入输出 | 分页参数是否设置合理最大值，避免不合理的深度分页或单页数据过多的情况发生，（如果参数配置不能由业务人员修改） | P0 | L1 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 十、安全 / 1. 输入输出 | 提供给前端或移动端使用的根据资源标识查询资源信息的 API，资源标识是否具备不可猜测性 | P0 | L2 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 十、安全 / 1. 输入输出 | XSS 攻击、SQL 注入风险 | P0 | L2 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 十、安全 / 2. 访问控制 | 短信炸弹：限制 ip、手机号操作频率 | P0 | L2 + Perf | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 十、安全 / 2. 访问控制 | 是否存在越权风险，主要是 API，操作者与被操作资源之间的权限判断应完整闭合。水平越权和垂直越权 | P0 | L2 | index.ts:285-288、341-344；service.ts:1125-1132、1589-1594。 | 涉及 | 服务层校验角色和 BA 资源归属，路由校验认证和操作权限；US1.8 service 未校验 BA 是否为需求归属人。 | TC1.5.3、TC1.8.5 | 待确认：测试案例正文只有编号和名称，无法核对步骤与预期结果 | 建议-CP-016：补充水平越权和垂直越权，L1 + L2。 | L2 API：认证、角色和归属权限断言 | 核对并修正 US1.8 BA 资源归属规则。 |
| 十、安全 / 3. 数据安全 | 新增打印日志是否对敏感信息进行脱敏处理 | P0 | L1 + M | service.ts:1177-1185、1613-1620；StatusLog data。 | 涉及 | 审计写入只记录业务字段，未见密码/token；仍需核对日志输出规范。 | TC1.5.6、TC1.8.3 | 待确认：测试案例正文只有编号和名称，无法核对步骤与预期结果 | 无 | L2 API：审计字段未含密码/token；无日志扫描 | 已读取审计字段；未执行日志扫描。 |
| 十、安全 / 3. 数据安全 | 出入参是否包含高等级数据或敏感数据，需进行字段级加密 | P0 | L1 + M | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 十、安全 / 3. 数据安全 | 响应数据是否存在暴露内部资源地址、内部网络信息等风险 | P1 | L2 | service.ts:1188-1200、1623-1635；index.ts:294-295、350-351。 | 待确认 | 接口返回 RequirementDetail，包含用户、状态日志等字段；尚未完成安全响应字段审查。 | TC1.5.1、TC1.8.1 | 待确认：前端、案例正文或安全口径未完整纳入 | 建议-CP-017：补充响应字段白名单和内部信息泄露检查，L2。 | L2 API + L3 UI：响应被使用；无字段安全白名单断言 | 确认面向前端的响应字段清单。 |
| 十、安全 / 3. 数据安全 | 面客功能展示敏感信息时，是否根据行内相关要求进行脱敏，且脱敏行为不是前端进行的 | P0 | L3 | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
| 十、安全 / 4. 下发安全 | 生产数据、脚本、权限修改应附带备份步骤，避免修改动作发生异常时无法回退 | P1 | M | 本轮逻辑代码为 requirements/service.ts、requirements/index.ts、shared/constants/index.ts，未发现对应实现。 | 不涉及 | 本轮确认的逻辑代码范围未发现该类行为，或检查点属于未纳入的前端/文件/运维/数据库/交易领域。 | 无 | 不适用 | 无 | 不适用：代码不涉及 | 无 |
