# US2.3 班次列表查询与火车切换 UI 自动化覆盖报告

## 1. 结论摘要

| 项目 | 结论 |
|---|---|
| UI 自动化覆盖状态 | 缺失 |
| 执行结果 | 未执行 |
| 是否建议人工替代 | 是，但本次用户已确认不启用人工验证 |
| 主要风险 | `/schedules` 页面入口、火车切换、班次列表展示、状态操作按钮、跳转详情等关键前端流程缺少自动化或人工证据 |

## 2. 执行信息

| 项目 | 内容 |
|---|---|
| 前端仓库/分支 | `release-train/apps/web`；分支未在本轮指定 |
| 浏览器 | 未执行 |
| UI 自动化框架 | 仓库存在 `tests/e2e` 示例，未发现 US2.3 对应脚本 |
| 执行报告 | 缺失 |
| 截图/视频 | 缺失 |

## 3. UI 流程覆盖矩阵

| 页面/流程 | 关联需求 | 测试案例 | 自动化脚本 | 结果 | 缺口 |
|---|---|---|---|---|---|
| `/schedules` 班次列表入口 | US2.3 当前确认入口 | FE-01、FE-02 | 缺失 | 未执行 | 未验证页面可正常进入、列表加载、分页展示 |
| 火车下拉与火车切换 | US2.3 火车切换 | FE-03、FE-04、FE-05 | 缺失 | 未执行 | 未验证切换火车后列表刷新、空数据提示、接口调用行为 |
| 全部火车班次列表 | US2.3 班次列表查询 | FE-06、FE-07 | 缺失 | 未执行 | 未验证默认列表、分页、状态展示、数量字段展示 |
| 班次详情跳转 | US2.3 查看班次详情入口 | FE-08 | 缺失 | 未执行 | 未验证点击行跳转到 `/trains/{trainId}/schedules/{scheduleId}` |
| 创建/编辑班次入口 | US2.3 页面操作链路 | FE-09、FE-10 | 缺失 | 未执行 | 未验证未选择火车时提示、选择火车后弹窗行为 |
| 状态操作按钮 | US2.3 班次状态操作 | FE-11、FE-12、GAP-03 | 缺失 | 未执行 | 代码仅按状态控制按钮，未按角色隐藏；需要 UI 或人工证据确认风险 |
| 所属火车列展示 | 详细设计差距项 | GAP-01 | 缺失 | 未执行 | 后端返回 `trainName`，前端当前列表列未展示所属火车 |
| 选中火车后的分页 | 详细设计差距项 | GAP-02 | 缺失 | 未执行 | 选中火车后走 `/trains/{trainId}/schedules`，前端本地计算分页，后端接口未分页 |

## 4. 人工替代建议

| 场景 | 是否可人工替代 | 原因 | 证据要求 |
|---|---|---|---|
| `/schedules` 页面进入与列表展示 | 是 | UI 自动化成本较高，人工验证能直接确认页面可用性 | 页面截图 |
| 火车切换后列表刷新 | 是 | 需要观察页面状态变化和接口返回 | 切换前后截图，必要时浏览器网络请求截图 |
| 班次详情跳转 | 是 | 只需验证关键跳转链路 | 跳转前后截图 |
| 状态操作按钮权限展示 | 是 | 需用不同角色确认按钮可见性 | 不同账号页面截图 |
| 资金类交易/数据库变化 | 不适用 | 本需求不涉及资金类交易 | 不适用 |

本轮用户已明确“本次不启用人工”，因此不创建 evidence 目录，也不要求补充截图。

## 5. 补测 TODO

| 优先级 | TODO | 建议脚本/方式 | 说明 |
|---|---|---|---|
| P1 | 新增 `/schedules` 页面可进入、列表加载、分页展示 UI 自动化 | Playwright | 覆盖 US2.3 主入口 |
| P1 | 新增火车切换后列表刷新 UI 自动化 | Playwright | 覆盖核心交互 |
| P1 | 新增详情跳转 UI 自动化 | Playwright | 验证列表到详情链路 |
| P1 | 验证状态操作按钮权限展示 | Playwright 或人工 | 当前代码存在仅按状态展示按钮的风险 |
| P2 | 验证所属火车列展示 | Playwright 或人工 | 当前前端未展示 `trainName`，属于设计差距 |
| P2 | 验证选中火车后的分页行为 | Playwright 或 API 结合人工 | 当前接口/前端分页口径与详细设计存在差距 |

## 6. 给最终报告的摘要

- L3 UI 覆盖：缺失，未发现 US2.3 相关 UI 自动化脚本，本轮未执行 UI 自动化。
- 人工验证：用户确认本次不启用人工验证，因此 UI 关键流程暂无人工证据补位。
- 主要风险：`/schedules` 入口、火车切换、详情跳转、权限按钮、所属火车列、选中火车分页均缺少 UI 侧执行证据。