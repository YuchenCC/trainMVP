# Handoff Document

## Purpose

交接当前 `test-strategy-planner` skill 的规则整理、输出模板调整和飞书 MCP 接入配置，供下一次会话继续验证或提交变更。

## Completed Work

- 重整 `test-strategy-planner/SKILL.md` 的读取规则：
  - 测试案例必须先确认。
  - 核心规则已并入“工作流程与核心规则”。
  - 小模型章节只保留触发条件和 `small-model-reading.md` 引用。
  - 详细测试案例索引方法只放在 `references/small-model-reading.md`。
- 调整 `references/output-template.md`：
  - 主报告保留分析范围、分层方案、需求-案例矩阵、自测检查点、覆盖现状、TODO、人工验证和风险。
  - “覆盖现状”调整为第 5 节。
  - 代码影响和现有测试资产合并到附件“代码与现有测试资产索引”。
  - 删除“不建议放进单测的内容”和“单测可以考虑的点”。
  - 删除主报告独立的“后续 skill 动态推荐”，合并到 TODO 的“推荐 skill”列。
  - 人工验证附件改为“已确认的人工验证替代 UI 场景”，不再重复询问用户是否创建 evidence。
- 在 `SKILL.md` 增加生成文件后的飞书操作规则：
  - 报告生成并完成自检后，询问是否创建飞书文档和“自测策略确认”任务。
  - 必须通过飞书 MCP 查询审批人；只有一个候选人也必须再次确认。
  - 未确认前不创建、不发送；创建后验证文档链接、任务标识、审批人和状态。
- 更新 `references/lark-document-source.md`：
  - 增加统一的最小 scope 清单。
  - 覆盖 Docx/Wiki 读取、文档导入、文档分享、用户检索、普通任务、原生审批和 OAuth 刷新。
  - 记录 tenant scope 与 user OAuth scope 的区别。
  - 增加一键授权命令和令牌过期处理规则。
- 新增 `references/README-lark-mcp.md`，说明飞书应用、Codex MCP、重定向 URL、环境变量、授权和验证流程。
- 将 MCP 启动器和一键授权脚本迁移到 skill 的 `references/`：
  - `references/run-lark-mcp.mjs`
  - `references/login-test-strategy-planner.cmd`
- 已更新 `C:\Users\Administrator\.codex\config.toml`，Lark MCP 启动器指向 skill reference 下的 `run-lark-mcp.mjs`。
- 已删除旧的 `.codex/packages/lark-mcp/run-lark-mcp.mjs` 和 `.codex/packages/lark-mcp/login-test-strategy-planner.cmd`。
- 已通过 `quick_validate.py`、Node `--check` 和配置一致性检查；未真实创建飞书文档或审批任务。

## Pending Tasks

- 重启 Codex，使新的 `config.toml` 启动路径生效。
- 运行一键授权：

  ```powershell
  & "E:\CodexTest\trainMVP\.agents\skills\test-strategy-planner\references\login-test-strategy-planner.cmd"
  ```

- 重新授权后，用只读 Lark MCP 调用验证 Docx/Wiki 搜索和用户搜索。
- 确认当前 Lark MCP 是否实际暴露用户搜索、普通任务创建、审批创建工具；权限齐全不代表 MCP 已暴露对应工具。
- 如需真实创建文档、普通任务或审批实例，必须先获得用户当轮明确确认，再执行并验证结果。
- 评估是否将 `approval:instance`、`approval:approval:readonly`、`approval:task` 保持在应用 tenant scope 中；这些不能作为用户 OAuth scope 传入登录脚本。
- 处理当前工作区已有的 Git staged/worktree 混合状态，提交前先检查差异，不要覆盖用户已有修改。

## Key Decisions

- 不复用存量报告，除非用户明确确认；单测治理报告是否复用也必须单独确认。
- `self-test-checkpoints.md` 是主报告的自测检查点输入，不能替代测试案例覆盖结论。
- 内部索引表只供 AI 读取和自检，默认不输出到报告。
- 主报告采用当前完整模板；此前讨论的“审核版精简结构”未被确认，不得擅自切换。
- 代码影响和现有测试资产不再作为主报告独立章节，统一放入附件执行索引。
- 人工验证是否启用必须在前置流程确认一次，附件只记录确认后的场景。
- 飞书 OAuth 不把 App ID、App Secret、授权码或 token 写入 skill、reference、报告或最终回复。
- 一键授权脚本只请求统一 user OAuth scope；原生审批相关 scope 由应用 tenant 权限配置提供。

## Relevant Artifacts

- [test-strategy-planner/SKILL.md](E:/CodexTest/trainMVP/.agents/skills/test-strategy-planner/SKILL.md)
- [small-model-reading.md](E:/CodexTest/trainMVP/.agents/skills/test-strategy-planner/references/small-model-reading.md)
- [output-template.md](E:/CodexTest/trainMVP/.agents/skills/test-strategy-planner/references/output-template.md)
- [lark-document-source.md](E:/CodexTest/trainMVP/.agents/skills/test-strategy-planner/references/lark-document-source.md)
- [README-lark-mcp.md](E:/CodexTest/trainMVP/.agents/skills/test-strategy-planner/references/README-lark-mcp.md)
- [run-lark-mcp.mjs](E:/CodexTest/trainMVP/.agents/skills/test-strategy-planner/references/run-lark-mcp.mjs)
- [login-test-strategy-planner.cmd](E:/CodexTest/trainMVP/.agents/skills/test-strategy-planner/references/login-test-strategy-planner.cmd)
- `C:\Users\Administrator\.codex\config.toml`

## Suggested Skills for Next Session

- `test-strategy-planner`：继续验证实际需求分析、报告生成和人工确认门禁。
- `skill-creator`：继续校验和迭代 skill 结构。
- `handoff`：需要再次交接时生成新的日期版本。

## Context

- 当前 Lark MCP 曾经成功读取 Feishu Wiki/Docx，但后续只读用户接口返回过 `user_access_token is invalid or expired`，需要重新 OAuth。
- 之前实际暴露的 Lark 工具包括 Docx 搜索/正文读取、Wiki 搜索/节点解析、文档导入、权限成员操作和群成员相关能力；当时未发现独立通讯录用户搜索、任务创建或审批创建工具。
- `login-lark-mcp.cmd` 仍位于 `.codex/packages/lark-mcp/`，但它是未指定 scope 的通用登录脚本；当前 skill 应使用 references 下的专用授权脚本。
- 不要在下一次会话中直接创建飞书外部资源，先验证 token、工具 schema 和用户确认门禁。
